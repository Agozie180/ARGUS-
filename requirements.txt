# --- Core runtime (required to run main.py and the backtest) ---
pydantic>=2.5
pydantic-settings>=2.1
rich>=13.7

# --- API server (backend.main:app) ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Backtest & dashboard ---
pandas>=2.1
numpy>=1.26
matplotlib>=3.8
plotly>=5.18
streamlit>=1.30

# --- Tests ---
pytest>=8.0

# NOTE: Heavy/optional ML deps (chromadb, sentence-transformers, litellm) are NOT
# installed by default — they pull in torch (~1-2 GB), which slows cloud builds
# and can OOM free-tier containers (a common cause of blank deploys). The app
# degrades gracefully without them: vector memory becomes a no-op and
# reflection/decision fall back to deterministic rules. The live dashboard, the
# API, and the Bitget integration never need them. To enable LLM/vector features:
#     pip install -r requirements-ml.txt
