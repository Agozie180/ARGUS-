# Optional ML extras for Argus — NOT required to run the dashboard, API, or the
# live Bitget integration. These pull in torch (~1-2 GB); install only if you
# want vector memory and LLM-enriched reflection. Everything degrades gracefully
# when these are absent.
-r requirements.txt

# Vector memory / reflection
chromadb>=0.4.22
sentence-transformers>=2.3.0

# LLM reasoning (falls back to deterministic rules if absent)
litellm>=1.30.0
